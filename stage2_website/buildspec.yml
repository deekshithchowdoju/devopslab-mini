version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
  build:
    commands:
      - echo "Build started on $(date)"
      - echo "Deploying website to S3..."
      - aws s3 sync . s3://devopslab-mini-stage2 --delete

artifacts:
  files:
    - '**/*'
